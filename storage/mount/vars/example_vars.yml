# ⚠️ 重要提示：所有变量均为占位符，请在安全的实验环境中再替换为真实路径或设备。
# 建议使用 Ansible Vault/环境变量保管真实挂载点信息，勿将生产配置提交到版本库。
storage_work_dir: "{{ lookup('env', 'HOME') }}/.ansible_storage_demo"
# loopback 镜像文件，示例放在用户家目录下便于无 root 权限创建
storage_loop_file: "{{ storage_work_dir }}/loop-mount.img"
# 生成镜像的大小（单位：MiB），请根据可用磁盘空间调整
loopback_size_mb: 8
# 🔐 仅用于演示的 loop 设备占位符，真实环境请改成 losetup 输出的路径
storage_loop_device: "/dev/loop_demo"
# 挂载点请选择临时目录，避免覆盖业务挂载
storage_mount_point: "/tmp/mount_demo"
# 建议与 filesystem 模块示例保持一致的文件系统类型
storage_fs_type: "ext4"
# 可在此处追加 ro/noexec 等限制参数，保障实验可控
storage_mount_options: "defaults,noatime"
# 在无权限写系统 fstab 时，可自定义一个临时文件路径
storage_fstab_path: "/tmp/fstab_demo"
